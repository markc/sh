# 2026-05-19 — CLAUDE.md/README accuracy pass

## Changes

- Synced `CLAUDE.md` (the symlinked README) with the actual `_shrc` and `sshm`
  behaviour rather than stale approximations:
  - Corrected line counts: `_shrc` 368 (was ~324), `sshm` 695 (was ~706).
  - Noted `README.md` is a symlink to `CLAUDE.md` — one document, not two.
  - Documented that `_shrc` prepends `~/.sh` to `PATH` so `sshm` is callable
    after sourcing.
  - Expanded `OSTYP` to include `ubuntu`/`manjaro` and the `ID_LIKE` → `uname`
    fallback chain; noted `ARCH` falls back to raw `uname -m`.
  - Filled out OS abstraction: package backends (apt/apk/opkg/paru/brew),
    service backends (systemd/OpenRC/init.d/launchctl), and the Arch
    `u`/`ua`/`uu`/`uc` upgrade variants.
  - Rewrote the aliases/functions list to match what actually ships
    (Navigation, Search, Diagnostics, Packages, Services, Users, SSH,
    Notes/help, Misc) including `q`, `temp`, `getusers`, `chktime`,
    `shortname`, etc.
  - Clarified `sshm` notes: `push` will push existing commits even with
    nothing new to commit; `init` is idempotent on an existing `~/.bashrc`.
  - Updated server module command lists (server.sh/logs.sh/net.sh) to the
    full current set.
  - Added an "Everything versioned" convention and Manjaro to the platforms
    line; minor whitespace/wording fixes.

## Notes

- Pure documentation change — no behavioural code touched. `_shrc` and `sshm`
  were the source of truth; the doc was brought up to them.
